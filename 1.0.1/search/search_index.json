{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Timtek.GitFlowVersioning","text":"<p>Automatic semantic versioning for .NET projects that follow GitFlow \u2014 zero configuration required.</p>"},{"location":"#why-this-exists","title":"Why This Exists","text":"<p>Most .NET versioning tools try to be everything to everyone. They support dozens of branching strategies, hundreds of configuration options, and require significant setup before they produce a useful version number. For teams that have already committed to GitFlow, all of that flexibility is overhead.</p> <p><code>Timtek.GitFlowVersioning</code> takes the opposite approach. It is an opinionated tool that delivers a single, specific use-case and deliberately excludes everything else:</p> <p>If you use GitFlow and .NET SDK-style projects, install the package and you are done.</p> <p>There is no configuration file, no YAML schema to learn, no command-line flags to remember. The tool reads your Git history, classifies your branch, and stamps every build artefact with the correct SemVer 2.0 version \u2014 automatically.</p>"},{"location":"#design-philosophy","title":"Design Philosophy","text":"Principle What it means in practice Zero configuration Install the NuGet package. There is nothing else to do. Convention over configuration Branch names are the configuration. <code>main</code> is stable, <code>develop</code> is alpha, <code>release/*</code> is beta. Opinionated scope Only GitFlow is supported. If you use trunk-based development or another model, this is not the right tool. Never break a build If the tool cannot compute a version (no Git, shallow clone, missing tags), it logs a warning and falls back to <code>0.0.0-unversioned</code>. No runtime dependency The package is a <code>DevelopmentDependency</code>. It participates only at build time and adds nothing to your deployed application."},{"location":"#at-a-glance","title":"At a Glance","text":"<pre><code># Install \u2014 this is the only step\ndotnet add package Timtek.GitFlowVersioning\n</code></pre> <p>From this point on, every <code>dotnet build</code> and <code>dotnet pack</code> produces correctly versioned assemblies and NuGet packages based on your Git branch and tags.</p>"},{"location":"#what-gets-set","title":"What Gets Set","text":"<p>The MSBuild task sets all of the standard .NET version properties:</p> <ul> <li><code>Version</code> / <code>PackageVersion</code> \u2014 the SemVer string used by NuGet</li> <li><code>AssemblyVersion</code> \u2014 the four-part assembly version</li> <li><code>FileVersion</code> \u2014 the Win32 file version</li> <li><code>InformationalVersion</code> \u2014 the full version with build metadata</li> </ul> <p>It also generates a <code>GitVersionInformation</code> class containing every computed variable, available for runtime introspection via the <code>TA.Utils.Core</code> NuGet package.</p>"},{"location":"#who-is-this-for","title":"Who Is This For?","text":"<ul> <li>.NET teams using GitFlow with <code>main</code>, <code>develop</code>, <code>release/*</code>, and <code>hotfix/*</code> branches</li> <li>Projects built with SDK-style <code>.csproj</code> files and the <code>dotnet</code> CLI</li> <li>CI environments including GitHub Actions and TeamCity</li> </ul> <p>If your workflow matches these conventions, you will never need to think about versioning again.</p> <p>Not for every project</p> <p>This tool deliberately does not support trunk-based development, custom branch naming, or version configuration files. If you need that flexibility, consider GitVersion or Nerdbank.GitVersioning which are excellent, broadly-scoped alternatives.</p>"},{"location":"ci-integration/","title":"CI Integration","text":"<p><code>Timtek.GitFlowVersioning</code> automatically detects supported CI environments and emits service messages so that the computed version is visible in build logs and available to downstream steps.</p> <p>No configuration is needed \u2014 detection is based on environment variables that CI systems set automatically.</p>"},{"location":"ci-integration/#github-actions","title":"GitHub Actions","text":"<p>When the <code>GITHUB_ACTIONS</code> environment variable is present, the task:</p> <ol> <li>Emits <code>::notice</code> annotations with the computed version values, visible in the    Actions job summary.</li> <li>Writes <code>semver</code>, <code>fullSemVer</code>, and <code>informationalVersion</code> to the    <code>$GITHUB_OUTPUT</code> file for use in subsequent steps.</li> </ol>"},{"location":"ci-integration/#using-outputs-in-workflow-steps","title":"Using Outputs in Workflow Steps","text":"<pre><code>- name: Build\n  id: build\n  run: dotnet build\n\n- name: Deploy\n  run: echo \"Deploying version ${{ steps.build.outputs.semver }}\"\n</code></pre>"},{"location":"ci-integration/#recommended-checkout-settings","title":"Recommended Checkout Settings","text":"<p>The versioning task uses <code>git describe</code> to locate the nearest tag. GitHub Actions performs a shallow clone by default, which may not include tag history. Always use <code>fetch-depth: 0</code>:</p> <pre><code>- name: Checkout\n  uses: actions/checkout@v4\n  with:\n    fetch-depth: 0\n</code></pre> <p>Shallow clones</p> <p>Without <code>fetch-depth: 0</code>, the task may not find version tags and will fall back to <code>0.0.0-unversioned</code>. This is the most common cause of unexpected versions in CI.</p>"},{"location":"ci-integration/#detached-head","title":"Detached HEAD","text":"<p>GitHub Actions checks out a specific commit (detached HEAD) rather than a branch. The task handles this by using <code>git name-rev</code> to determine the logical branch name.</p>"},{"location":"ci-integration/#teamcity","title":"TeamCity","text":"<p>When the <code>TEAMCITY_VERSION</code> environment variable is present, the task emits TeamCity service messages:</p> <pre><code>##teamcity[buildNumber '1.2.3-alpha.5+5']\n##teamcity[setParameter name='GitFlowVersion.SemVer' value='1.2.3-alpha.5']\n##teamcity[setParameter name='GitFlowVersion.FullSemVer' value='1.2.3-alpha.5+5']\n##teamcity[setParameter name='GitFlowVersion.InformationalVersion' value='1.2.3-alpha.5+5.Branch.develop.Sha.abc123...']\n</code></pre> <p>This automatically:</p> <ul> <li>Sets the build number in TeamCity to the full semantic version.</li> <li>Exposes <code>GitFlowVersion.SemVer</code>, <code>GitFlowVersion.FullSemVer</code>, and   <code>GitFlowVersion.InformationalVersion</code> as build parameters available to   subsequent build steps and dependent builds.</li> </ul>"},{"location":"ci-integration/#teamcity-configuration","title":"TeamCity Configuration","text":"<p>No special configuration is needed beyond ensuring that:</p> <ol> <li>The build checkout includes full history (not a shallow clone).</li> <li>Git is available on the agent's <code>PATH</code>.</li> </ol>"},{"location":"ci-integration/#other-ci-systems","title":"Other CI Systems","text":"<p>For CI systems that are not explicitly supported, the task still sets all MSBuild version properties. The version is visible in the build output log and can be captured from the <code>Version</code> or <code>PackageVersion</code> MSBuild properties.</p> <p>Alternatively, use the CLI tool to compute the version independently of MSBuild and pass it to downstream steps:</p> <pre><code>SEMVER=$(dotnet gitflowversion | jq -r '.SemVer')\ndotnet pack -p:PackageVersion=$SEMVER\n</code></pre> <p>See the CLI Tool page for full installation and usage details.</p> <p>If you need to extract the version purely from MSBuild, you can add a custom target that writes it to a file:</p> <pre><code>&lt;Target Name=\"WriteVersion\" AfterTargets=\"_ComputeGitFlowVersion\"&gt;\n  &lt;WriteLinesToFile File=\"$(IntermediateOutputPath)version.txt\"\n                    Lines=\"$(_GFV_SemVer)\"\n                    Overwrite=\"true\" /&gt;\n&lt;/Target&gt;\n</code></pre>"},{"location":"cli-tool/","title":"CLI Tool","text":"<p><code>Timtek.GitFlowVersion.Tool</code> is a standalone command-line tool that computes GitFlow-based semantic versions from Git history and outputs them as JSON. It uses the same versioning logic as the MSBuild task but can be run independently of a build \u2014 making it useful for CI scripting, diagnostics, and automation.</p>"},{"location":"cli-tool/#installation","title":"Installation","text":"<p>Install the tool as a local dotnet tool so it is available to anyone who clones the repository:</p> <pre><code>dotnet new tool-manifest   # only needed once per repository\ndotnet tool install Timtek.GitFlowVersion.Tool\n</code></pre> <p>This adds the tool to <code>.config/dotnet-tools.json</code>, which should be committed to source control. Other developers (and CI agents) can then restore it with:</p> <pre><code>dotnet tool restore\n</code></pre>"},{"location":"cli-tool/#usage","title":"Usage","text":"<p>Run the tool from any directory inside a Git repository:</p> <pre><code>dotnet gitflowversion\n</code></pre> <p>By default it uses the current working directory. You can pass an explicit path:</p> <pre><code>dotnet gitflowversion /path/to/repo\n</code></pre>"},{"location":"cli-tool/#output","title":"Output","text":"<p>The tool prints a JSON object containing every computed version variable:</p> <pre><code>{\n  \"Major\": \"1\",\n  \"Minor\": \"3\",\n  \"Patch\": \"0\",\n  \"MajorMinorPatch\": \"1.3.0\",\n  \"PreReleaseLabel\": \"alpha\",\n  \"PreReleaseLabelWithDash\": \"-alpha\",\n  \"PreReleaseNumber\": \"12\",\n  \"PreReleaseTag\": \"alpha.12\",\n  \"PreReleaseTagWithDash\": \"-alpha.12\",\n  \"SemVer\": \"1.3.0-alpha.12\",\n  \"FullSemVer\": \"1.3.0-alpha.12+12\",\n  \"BranchName\": \"develop\",\n  \"EscapedBranchName\": \"develop\",\n  \"Sha\": \"a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2\",\n  \"ShortSha\": \"a1b2c3d\",\n  \"BuildMetaData\": \"12\",\n  \"FullBuildMetaData\": \"12.Branch.develop.Sha.a1b2c3d4e5f6...\",\n  \"InformationalVersion\": \"1.3.0-alpha.12+12.Branch.develop.Sha.a1b2c3d4e5f6...\",\n  \"AssemblySemVer\": \"1.3.0.0\",\n  \"AssemblySemFileVer\": \"1.3.0.0\"\n}\n</code></pre> <p>The variables are identical to those produced by the MSBuild task. See the Version Variables page for a complete reference.</p>"},{"location":"cli-tool/#extracting-values-in-scripts","title":"Extracting Values in Scripts","text":"<p>The JSON output is designed to be parsed with standard tools like <code>jq</code>:</p> BashPowerShell <pre><code>SEMVER=$(dotnet gitflowversion | jq -r '.SemVer')\necho \"Version is $SEMVER\"\n</code></pre> <pre><code>$version = dotnet gitflowversion | ConvertFrom-Json\nWrite-Host \"Version is $($version.SemVer)\"\n</code></pre>"},{"location":"cli-tool/#ci-usage","title":"CI Usage","text":"<p>The tool is particularly useful in CI pipelines where you need the version before or independently of the MSBuild build. A typical GitHub Actions pattern:</p> <pre><code>- name: Checkout\n  uses: actions/checkout@v4\n  with:\n    fetch-depth: 0\n\n- name: Setup .NET\n  uses: actions/setup-dotnet@v4\n  with:\n    dotnet-version: 9.x\n\n- name: Restore tools\n  run: dotnet tool restore\n\n- name: Compute version\n  id: version\n  run: |\n    VERSION_JSON=$(dotnet gitflowversion)\n    SEMVER=$(echo \"$VERSION_JSON\" | jq -r '.SemVer')\n    echo \"semver=$SEMVER\" &gt;&gt; \"$GITHUB_OUTPUT\"\n    echo \"::notice title=GitFlowVersion::SemVer=$SEMVER\"\n\n- name: Build\n  run: dotnet build --configuration Release\n\n- name: Pack\n  run: &gt;\n    dotnet pack --configuration Release --output nupkgs\n    -p:PackageVersion=${{ steps.version.outputs.semver }}\n</code></pre> <p>Bootstrapping</p> <p>The CLI tool solves a bootstrapping problem: a repository that produces the versioning MSBuild task cannot consume its own package to version itself. The tool provides the same version computation without depending on the MSBuild integration.</p>"},{"location":"cli-tool/#exit-codes","title":"Exit Codes","text":"Code Meaning <code>0</code> Success \u2014 version JSON was written to stdout. <code>1</code> Failure \u2014 an error message was written to stderr. <p>Unlike the MSBuild task (which never fails a build), the CLI tool returns a non-zero exit code on error so that CI scripts can detect and handle failures.</p>"},{"location":"cli-tool/#requirements","title":"Requirements","text":"<ul> <li>.NET 8.0 runtime or later.</li> <li>Git must be available on the <code>PATH</code>.</li> <li>The target directory must be inside a Git repository with at least one commit.</li> </ul>"},{"location":"faq/","title":"FAQ","text":""},{"location":"faq/#general","title":"General","text":""},{"location":"faq/#why-not-just-use-gitversion-or-nerdbankgitversioning","title":"Why not just use GitVersion or Nerdbank.GitVersioning?","text":"<p>Those are excellent, widely-used tools. They support many branching strategies, extensive configuration, and a broad range of scenarios.</p> <p><code>Timtek.GitFlowVersioning</code> exists because we wanted something simpler. Our projects all use GitFlow, and we found ourselves writing the same configuration files and debugging the same edge cases across dozens of repositories.</p> <p>This tool trades flexibility for simplicity: if you use GitFlow, install the package and you are done. No configuration files, no learning curve, no surprises.</p> <p>If your workflow differs from standard GitFlow or you need per-repository configuration, one of those tools is a better choice.</p>"},{"location":"faq/#what-branching-strategies-are-supported","title":"What branching strategies are supported?","text":"<p>Only GitFlow with the standard branch naming conventions:</p> <ul> <li><code>main</code> (or <code>master</code>)</li> <li><code>develop</code></li> <li><code>release/*</code></li> <li><code>hotfix/*</code></li> <li><code>feature/*</code>, <code>bugfix/*</code>, and any other prefix</li> </ul> <p>This is a deliberate design decision, not a limitation that will be \"fixed\" later.</p>"},{"location":"faq/#is-there-a-configuration-file","title":"Is there a configuration file?","text":"<p>No. The tool is zero-configuration by design. Branch names are the only input, and they follow the GitFlow convention.</p>"},{"location":"faq/#versioning","title":"Versioning","text":""},{"location":"faq/#where-does-the-base-version-come-from","title":"Where does the base version come from?","text":"<p>From the most recent Git tag that matches <code>*.*.*</code> (with or without a <code>v</code> prefix), located using <code>git describe --tags --long</code>.</p> <p>If no matching tag exists, the fallback base version is <code>0.1.0</code> and the commit distance is the total commit count in the repository.</p>"},{"location":"faq/#how-does-the-commit-distance-affect-the-version","title":"How does the commit distance affect the version?","text":"<p>It depends on the branch type:</p> Branch Effect of commit distance <code>main</code> Added to the patch component (<code>1.2.0</code> + 3 commits = <code>1.2.3</code>) <code>develop</code> Becomes the pre-release number (<code>1.3.0-alpha.5</code>) <code>release/*</code> / <code>hotfix/*</code> Becomes the pre-release number (<code>1.3.0-beta.4</code>) Other Becomes the pre-release number (<code>1.3.0-alpha.7</code>)"},{"location":"faq/#what-version-is-produced-on-develop","title":"What version is produced on <code>develop</code>?","text":"<p>The minor version is incremented by one from the base tag (anticipating the next release) and the patch is reset to zero. The commit distance becomes the <code>alpha</code> pre-release number:</p> <pre><code>Base tag: 1.2.0    Distance: 12    \u2192    1.3.0-alpha.12\n</code></pre>"},{"location":"faq/#can-i-override-the-version","title":"Can I override the version?","text":"<p>Yes. Pass <code>/p:Version=X.Y.Z</code> on the command line:</p> <pre><code>dotnet build /p:Version=2.0.0-custom.1\n</code></pre> <p>The task will not overwrite <code>Version</code> or <code>PackageVersion</code> if they have already been set explicitly.</p>"},{"location":"faq/#build-and-ide","title":"Build and IDE","text":""},{"location":"faq/#my-ide-shows-errors-about-gitversioninformation-not-existing","title":"My IDE shows errors about <code>GitVersionInformation</code> not existing","text":"<p>This is expected. The <code>GitVersionInformation</code> class is generated during compilation and does not exist in your source tree. The IDE's design-time analysis cannot see it.</p> <p>Use the <code>GitVersion</code> class from <code>TA.Utils.Core</code> for safe runtime access to version information.</p>"},{"location":"faq/#the-version-shows-000-unversioned","title":"The version shows <code>0.0.0-unversioned</code>","text":"<p>This means the task could not compute a version. Common causes:</p> <ol> <li> <p>Git is not on the <code>PATH</code>.    Verify by running <code>git --version</code> in a terminal.</p> </li> <li> <p>The directory is not a Git repository.    The task walks up from the project directory looking for a <code>.git</code> folder.</p> </li> <li> <p>Shallow clone in CI.    Use <code>fetch-depth: 0</code> in your checkout step.</p> </li> <li> <p>No commits in the repository.    The task requires at least one commit.</p> </li> </ol> <p>The task logs an MSBuild warning with the specific error. Check the build output for <code>Timtek.GitFlowVersioning: Failed to compute version:</code> messages.</p>"},{"location":"faq/#does-the-task-slow-down-my-build","title":"Does the task slow down my build?","text":"<p>The task runs a small number of <code>git</code> commands (typically 2\u20133) and generates two small source files. The overhead is negligible \u2014 usually under 100ms.</p>"},{"location":"faq/#does-it-work-with-multi-targeting-projects","title":"Does it work with multi-targeting projects?","text":"<p>Yes. The task runs during each inner (per-TFM) build and during the outer build for NuGet pack operations. Both <code>build/</code> and <code>buildMultiTargeting/</code> targets are included in the package.</p>"},{"location":"faq/#ci","title":"CI","text":""},{"location":"faq/#why-does-ci-produce-a-different-version-than-my-local-build","title":"Why does CI produce a different version than my local build?","text":"<p>The most common cause is a shallow clone. CI systems often clone only the most recent commit, which means <code>git describe</code> cannot find version tags.</p> <p>Ensure your CI checkout fetches full history:</p> <pre><code># GitHub Actions\n- uses: actions/checkout@v4\n  with:\n    fetch-depth: 0\n</code></pre>"},{"location":"faq/#does-the-task-detect-my-ci-system-automatically","title":"Does the task detect my CI system automatically?","text":"<p>Yes. GitHub Actions and TeamCity are detected via their standard environment variables (<code>GITHUB_ACTIONS</code> and <code>TEAMCITY_VERSION</code> respectively). No CI-specific configuration is needed.</p>"},{"location":"faq/#package","title":"Package","text":""},{"location":"faq/#does-this-add-a-runtime-dependency-to-my-application","title":"Does this add a runtime dependency to my application?","text":"<p>No. The package is marked as a <code>DevelopmentDependency</code> with <code>IncludeBuildOutput=false</code>. It participates only during MSBuild and does not add any DLLs or references to your application's output.</p>"},{"location":"faq/#can-i-use-this-with-net-framework-projects","title":"Can I use this with .NET Framework projects?","text":"<p>The MSBuild task targets .NET Standard 2.0, so it can be loaded by both the .NET SDK (<code>dotnet build</code>) and the full MSBuild (<code>msbuild.exe</code>) shipped with Visual Studio.</p> <p>SDK-style project files are required. Legacy <code>.csproj</code> files (with <code>&lt;Import&gt;</code> of <code>Microsoft.CSharp.targets</code>) are not supported.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<p>Add the package to your project:</p> <pre><code>dotnet add package Timtek.GitFlowVersioning\n</code></pre> <p>That is the only step. There are no configuration files to create, no properties to set, and no command-line flags to learn.</p> <p>Zero configuration</p> <p>The package is deliberately designed so that installation is the entire setup process. If you follow standard GitFlow conventions, everything works out of the box.</p>"},{"location":"getting-started/#what-happens-next","title":"What Happens Next","text":"<p>On the next <code>dotnet build</code>, the MSBuild task automatically:</p> <ol> <li>Detects the current Git branch and the most recent version tag.</li> <li>Computes a SemVer 2.0 version based on the branch type and commit distance.</li> <li>Sets <code>Version</code>, <code>PackageVersion</code>, <code>AssemblyVersion</code>, <code>FileVersion</code>, and <code>InformationalVersion</code>.</li> <li>Generates a <code>GitVersionInformation.g.cs</code> source file in the intermediate output directory.</li> </ol> <p>You can verify that versioning is active by checking the build output:</p> <pre><code>Timtek.GitFlowVersioning:\n  FullSemVer:           1.2.3-alpha.5+5\n  SemVer:               1.2.3-alpha.5\n  AssemblySemVer:       1.2.0.0\n  AssemblySemFileVer:   1.2.3.0\n  InformationalVersion: 1.2.3-alpha.5+5.Branch.develop.Sha.a1b2c3d...\n  BranchName:           develop\n  Sha:                  a1b2c3d4e5f6...\n</code></pre>"},{"location":"getting-started/#tagging-a-release","title":"Tagging a Release","text":"<p>Version numbers are derived from Git tags. Create a tag on <code>main</code> when you are ready to mark a release:</p> <pre><code>git tag 1.0.0\ngit push origin 1.0.0\n</code></pre> <p>Tags can use a <code>v</code> prefix (<code>v1.0.0</code>) \u2014 both formats are recognised.</p> <p>Tag placement matters</p> <p>Tags should be placed on <code>main</code> (or <code>master</code>). The commit distance from the nearest tag drives the patch number on <code>main</code> and the pre-release number on other branches.</p>"},{"location":"getting-started/#packaging","title":"Packaging","text":"<p>NuGet packages are versioned automatically:</p> <pre><code>dotnet pack\n</code></pre> <p>The resulting <code>.nupkg</code> will carry the computed <code>PackageVersion</code> without any manual <code>/p:Version=</code> overrides needed.</p>"},{"location":"getting-started/#requirements","title":"Requirements","text":"<ul> <li>Git must be available on the system <code>PATH</code>.</li> <li>The repository must have at least one commit.</li> <li>Projects must use SDK-style <code>.csproj</code> files (the modern format used by <code>dotnet new</code>).</li> </ul>"},{"location":"getting-started/#removing-or-disabling","title":"Removing or Disabling","text":"<p>To temporarily disable versioning without removing the package, set the MSBuild property:</p> Project fileCommand line <pre><code>&lt;PropertyGroup&gt;\n  &lt;GitFlowVersioningEnabled&gt;false&lt;/GitFlowVersioningEnabled&gt;\n&lt;/PropertyGroup&gt;\n</code></pre> <pre><code>dotnet build /p:GitFlowVersioningEnabled=false\n</code></pre> <p>To remove the package entirely:</p> <pre><code>dotnet remove package Timtek.GitFlowVersioning\n</code></pre>"},{"location":"how-it-works/","title":"How It Works","text":""},{"location":"how-it-works/#overview","title":"Overview","text":"<p><code>Timtek.GitFlowVersioning</code> ships as a NuGet package containing an MSBuild task. The task runs automatically before compilation and performs four steps:</p> <ol> <li>Inspect \u2014 reads the current branch name and runs <code>git describe</code> to find the nearest version tag.</li> <li>Classify \u2014 maps the branch name to a GitFlow category.</li> <li>Compute \u2014 calculates the full set of version variables from the branch type, base tag, and commit distance.</li> <li>Apply \u2014 sets MSBuild properties and generates source files so the compiler, assembly metadata, and NuGet pack all receive the correct version.</li> </ol> <p>The entire process is transparent. There are no configuration files and no decisions for the developer to make.</p>"},{"location":"how-it-works/#branch-classification","title":"Branch Classification","text":"<p>The tool recognises five branch categories:</p> Branch pattern Classification Pre-release label <code>main</code> or <code>master</code> Main (none \u2014 stable release) <code>develop</code> Develop <code>alpha</code> <code>release/*</code> Release <code>beta</code> <code>hotfix/*</code> Hotfix <code>beta</code> Everything else (<code>feature/*</code>, <code>bugfix/*</code>, etc.) Other <code>alpha</code> <p>Branch matching is case-insensitive. A branch named <code>Release/2.0</code> is treated identically to <code>release/2.0</code>.</p>"},{"location":"how-it-works/#version-computation","title":"Version Computation","text":""},{"location":"how-it-works/#base-version","title":"Base Version","text":"<p>The base version is the most recent Git tag that matches the pattern <code>*.*.*</code> (with or without a leading <code>v</code>/<code>V</code> prefix). The <code>git describe --tags --long</code> command is used to locate the tag and measure the commit distance from it.</p> <p>If no matching tag exists in the repository, the fallback base version is <code>0.1.0</code> and the commit distance is the total commit count.</p>"},{"location":"how-it-works/#main-branch","title":"Main Branch","text":"<p>On <code>main</code>, the version is a stable release with no pre-release label. The commit distance is added to the patch component of the base version:</p> <pre><code>Base tag: 1.2.0    Distance: 3    \u2192    Version: 1.2.3\n</code></pre> Variable Value <code>SemVer</code> <code>1.2.3</code> <code>AssemblySemVer</code> <code>1.2.0.0</code> <code>AssemblySemFileVer</code> <code>1.2.3.0</code> <code>InformationalVersion</code> <code>1.2.3+3.Branch.main.Sha.abc123...</code>"},{"location":"how-it-works/#develop-branch","title":"Develop Branch","text":"<p>On <code>develop</code>, the version is an alpha pre-release. The base version's minor component is incremented by one (anticipating the next release), the patch is reset to zero, and the commit distance becomes the pre-release number:</p> <pre><code>Base tag: 1.2.0    Distance: 12    \u2192    Version: 1.3.0-alpha.12\n</code></pre> Variable Value <code>SemVer</code> <code>1.3.0-alpha.12</code> <code>FullSemVer</code> <code>1.3.0-alpha.12+12</code> <code>PreReleaseLabel</code> <code>alpha</code> <code>PreReleaseNumber</code> <code>12</code>"},{"location":"how-it-works/#release-and-hotfix-branches","title":"Release and Hotfix Branches","text":"<p><code>release/*</code> and <code>hotfix/*</code> branches produce beta pre-releases. The base version is used as-is and the commit distance becomes the pre-release number:</p> <pre><code>Base tag: 1.3.0    Distance: 4    \u2192    Version: 1.3.0-beta.4\n</code></pre>"},{"location":"how-it-works/#feature-and-other-branches","title":"Feature and Other Branches","text":"<p>Any branch that does not match the patterns above is treated as an alpha pre-release, following the same formula as release/hotfix but with the <code>alpha</code> label:</p> <pre><code>Base tag: 1.3.0    Distance: 7    \u2192    Version: 1.3.0-alpha.7\n</code></pre>"},{"location":"how-it-works/#tag-format","title":"Tag Format","text":"<p>Tags can use either bare versions or a <code>v</code> prefix:</p> <ul> <li><code>1.0.0</code> \u2713</li> <li><code>v1.0.0</code> \u2713</li> <li><code>V2.1.0</code> \u2713</li> </ul> <p>The leading <code>v</code>/<code>V</code> is stripped during parsing. Three-part <code>Major.Minor.Patch</code> is expected.</p>"},{"location":"how-it-works/#msbuild-integration","title":"MSBuild Integration","text":"<p>The package includes <code>.props</code> and <code>.targets</code> files in the standard NuGet layout:</p> <pre><code>build/\n  Timtek.GitFlowVersioning.props      \u2190 disables SDK assembly info generation\n  Timtek.GitFlowVersioning.targets    \u2190 defines the versioning target\nbuildTransitive/\n  ...                                 \u2190 same files for transitive consumers\nbuildMultiTargeting/\n  ...                                 \u2190 outer-build support for multi-TFM projects\ntasks/\n  netstandard2.0/\n    Timtek.GitFlowVersioning.dll      \u2190 the MSBuild task assembly\n</code></pre> <p>The <code>_ComputeGitFlowVersion</code> target runs <code>BeforeTargets=\"CoreCompile;GenerateAssemblyInfo;BeforeBuild\"</code>, ensuring version properties are set before any compilation occurs.</p>"},{"location":"how-it-works/#design-time-builds","title":"Design-Time Builds","text":"<p>The target is automatically skipped during design-time builds (<code>$(DesignTimeBuild) == 'true'</code>) to avoid interfering with IDE responsiveness.</p>"},{"location":"how-it-works/#generated-source-files","title":"Generated Source Files","text":"<p>Two files are generated in the intermediate output directory (<code>obj/</code>):</p>"},{"location":"how-it-works/#gitversioninformationgcs","title":"<code>GitVersionInformation.g.cs</code>","text":"<p>An <code>internal static</code> class containing <code>const string</code> fields for every version variable. This class is compiled into your assembly but is not visible in your source tree \u2014 it exists only in the intermediate output.</p> <p>IDE may show errors</p> <p>Because <code>GitVersionInformation</code> does not exist until compilation, the IDE may report errors if you reference it directly. Use the <code>GitVersion</code> class from <code>TA.Utils.Core</code> for safe runtime access.</p>"},{"location":"how-it-works/#timtekgitflowversioningassemblyinfogcs","title":"<code>Timtek.GitFlowVersioning.AssemblyInfo.g.cs</code>","text":"<p>Contains <code>[assembly: AssemblyVersion]</code>, <code>[assembly: AssemblyFileVersion]</code>, and <code>[assembly: AssemblyInformationalVersion]</code> attributes. The SDK's own assembly info generation is disabled by the <code>.props</code> file to avoid conflicts.</p>"},{"location":"how-it-works/#fault-tolerance","title":"Fault Tolerance","text":"<p>The task is designed to never fail a build. If it cannot compute a version for any reason \u2014 Git is not installed, the directory is not a repository, the history is unreadable, or any other unexpected error occurs \u2014 it:</p> <ol> <li>Logs an MSBuild warning (not an error).</li> <li>Substitutes the placeholder version <code>0.0.0-unversioned</code>.</li> <li>Returns <code>true</code> from the task so the build continues.</li> </ol> <p>This ensures that versioning issues surface as visible warnings without blocking development or CI pipelines.</p>"},{"location":"version-variables/","title":"Version Variables","text":"<p><code>Timtek.GitFlowVersioning</code> computes a comprehensive set of version variables on every build. These are exposed in three ways:</p> <ol> <li>MSBuild properties \u2014 available during the build for use in targets, conditions, and pack metadata.</li> <li>Generated <code>GitVersionInformation</code> class \u2014 compiled into your assembly as <code>internal static</code> constants.</li> <li>Standard .NET assembly attributes \u2014 <code>AssemblyVersion</code>, <code>AssemblyFileVersion</code>, and <code>AssemblyInformationalVersion</code>.</li> </ol>"},{"location":"version-variables/#complete-variable-reference","title":"Complete Variable Reference","text":"<p>The following example assumes a repository on the <code>develop</code> branch with the most recent tag <code>1.2.0</code> and 5 commits since that tag, at commit <code>a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2</code>.</p> Variable MSBuild Property Example Value Description <code>Major</code> <code>_GFV_Major</code> <code>1</code> Major version number <code>Minor</code> <code>_GFV_Minor</code> <code>3</code> Minor version number <code>Patch</code> <code>_GFV_Patch</code> <code>0</code> Patch version number <code>MajorMinorPatch</code> <code>_GFV_MajorMinorPatch</code> <code>1.3.0</code> Three-part version string <code>PreReleaseLabel</code> <code>_GFV_PreReleaseLabel</code> <code>alpha</code> Pre-release label (<code>alpha</code>, <code>beta</code>, or empty) <code>PreReleaseLabelWithDash</code> <code>_GFV_PreReleaseLabelWithDash</code> <code>-alpha</code> Pre-release label with leading dash <code>PreReleaseNumber</code> <code>_GFV_PreReleaseNumber</code> <code>5</code> Commit distance used as pre-release number <code>PreReleaseTag</code> <code>_GFV_PreReleaseTag</code> <code>alpha.5</code> Full pre-release tag <code>PreReleaseTagWithDash</code> <code>_GFV_PreReleaseTagWithDash</code> <code>-alpha.5</code> Full pre-release tag with leading dash <code>SemVer</code> <code>_GFV_SemVer</code> <code>1.3.0-alpha.5</code> SemVer 2.0 version string <code>FullSemVer</code> <code>_GFV_FullSemVer</code> <code>1.3.0-alpha.5+5</code> SemVer with build metadata <code>BranchName</code> <code>_GFV_BranchName</code> <code>develop</code> Current Git branch name <code>EscapedBranchName</code> <code>_GFV_EscapedBranchName</code> <code>develop</code> Branch name with <code>/</code> replaced by <code>-</code> <code>Sha</code> <code>_GFV_Sha</code> <code>a1b2c3d4e5f6...</code> Full commit SHA <code>ShortSha</code> <code>_GFV_ShortSha</code> <code>a1b2c3d</code> First 7 characters of the commit SHA <code>BuildMetaData</code> <code>_GFV_BuildMetaData</code> <code>5</code> Commit distance as build metadata <code>FullBuildMetaData</code> <code>_GFV_FullBuildMetaData</code> <code>5.Branch.develop.Sha.a1b2c3d...</code> Full build metadata string <code>InformationalVersion</code> <code>_GFV_InformationalVersion</code> <code>1.3.0-alpha.5+5.Branch.develop.Sha.a1b2c3d...</code> Complete informational version <code>AssemblySemVer</code> <code>_GFV_AssemblySemVer</code> <code>1.3.0.0</code> Four-part assembly version <code>AssemblySemFileVer</code> <code>_GFV_AssemblySemFileVer</code> <code>1.3.0.0</code> Four-part file version"},{"location":"version-variables/#standard-msbuild-properties","title":"Standard MSBuild Properties","text":"<p>In addition to the <code>_GFV_*</code> properties, the task sets the following standard MSBuild properties that are consumed by the compiler, NuGet pack, and assembly info generation:</p> MSBuild Property Source Variable Purpose <code>Version</code> <code>SemVer</code> Used by NuGet for package version <code>PackageVersion</code> <code>SemVer</code> Explicit NuGet package version <code>AssemblyVersion</code> <code>AssemblySemVer</code> Stamped into <code>[AssemblyVersion]</code> <code>FileVersion</code> <code>AssemblySemFileVer</code> Stamped into <code>[AssemblyFileVersion]</code> <code>InformationalVersion</code> <code>InformationalVersion</code> Stamped into <code>[AssemblyInformationalVersion]</code> <p>Explicit version override</p> <p>If you pass <code>/p:Version=X.Y.Z</code> on the command line (for example in a CI script), the task will not override <code>Version</code> or <code>PackageVersion</code>. This allows CI pipelines to pin a specific version when needed.</p>"},{"location":"version-variables/#using-variables-in-msbuild","title":"Using Variables in MSBuild","text":"<p>The <code>_GFV_*</code> properties are available after the <code>_ComputeGitFlowVersion</code> target runs. You can reference them in your project file for custom targets:</p> <pre><code>&lt;Target Name=\"PrintVersion\" AfterTargets=\"_ComputeGitFlowVersion\"&gt;\n  &lt;Message Importance=\"High\" Text=\"Building version $(_GFV_FullSemVer)\" /&gt;\n&lt;/Target&gt;\n</code></pre>"},{"location":"version-variables/#runtime-access","title":"Runtime Access","text":"<p>The generated <code>GitVersionInformation</code> class is <code>internal</code> and does not exist until compilation, so direct source references will produce IDE errors.</p> <p>Use the <code>GitVersion</code> class from the <code>TA.Utils.Core</code> NuGet package for safe, typed access at runtime:</p> <pre><code>var version = GitVersion.GitInformationalVersion;\nConsole.WriteLine(version);  // \"1.2.3+5.Branch.main.Sha.a1b2c3d...\"\n</code></pre>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/","title":"A Minimal, Opinionated, GitFlow-focused Versioning Utility for .NET)","text":"<p>Document status: Draft design spec\u00a0</p> <p>Audience: .NET build/release engineers; tooling developers\u00a0</p> <p>Primary goal: A pared-down, GitVersion-like versioning utility optimized for GitFlow and MSBuild, distributed as a NuGet package, working consistently in Visual Studio, dotnet build, TeamCity (Windows/Linux), and GitHub Actions.</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#1-background-motivation","title":"1. Background &amp; Motivation","text":"<p>GitVersion demonstrated that Git history + branching conventions can produce deterministic, semver-compatible versions, and that those versions can be injected into builds (assemblies, packages, and generated code) with minimal build script work. GitVersion\u2019s MSBuild approach includes generating version attributes during build (e.g., <code>AssemblyVersion</code>, <code>AssemblyFileVersion</code>, <code>AssemblyInformationalVersion</code>). 1</p> <p>GitVersion also exposes a stable set of named \u201cversion variables\u201d (e.g., <code>FullSemVer</code>, <code>AssemblySemVer</code>, <code>InformationalVersion</code>) with documented meanings and default formats. 2</p> <p>GitFlow-specific semantics (e.g., main releases, develop alphas, hotfix/release betas) are well described in GitVersion\u2019s GitFlow strategy documentation, including how branches and tags are treated. 3</p> <p>This spec proposes an in-house tool that keeps only the capabilities needed for:</p> <ul> <li>GitFlow branch naming and behavior</li> <li>deterministic unique version per commit</li> <li>assembly + NuGet package versioning</li> <li>build log output</li> <li>injected <code>GitVersionInformation</code> class compatibility</li> <li>TeamCity build numbering</li> </ul>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#2-scope","title":"2. Scope","text":""},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#21-in-scope-requirements-must","title":"2.1 In-Scope Requirements (Must)","text":"<p>AXIOM: All builds with different source inputs shall have different version strings. This requires that every commit produces a different semantic version string.</p> <p>AXIOM: A build shall never fail due to this utility. If for any reason a version string cannot be computed, an explanation shall be emitted into the logs and the version shall be \"0.0.0-unversioned\".</p> <p>The tool MUST:</p> <ol> <li>Support GitFlow branching strategy (main/master + develop + feature/ + release/ + hotfix/*). 34 </li> <li>Be deployable via NuGet package(s) and usable as a development dependency.</li> <li>Work in: \u00a0\u00a0 - Visual Studio builds (desktop MSBuild) \u00a0\u00a0 - dotnet build builds (MSBuild on .NET) \u00a0\u00a0 - TeamCity agents on Windows and Linux \u00a0\u00a0 - GitHub Actions</li> <li>Require no modifications to individual <code>.csproj</code> files ideally: \u00a0\u00a0 - Preferred installation is repo-level (<code>Directory.Build.props</code> / central package management), not per-project edits.</li> <li>Output computed version strings to the build log every build.</li> <li>Version NuGet packages produced by <code>dotnet pack</code>.</li> <li>Version-stamp assemblies (version attributes).</li> <li>Generate an injected static class compatible with GitVersion: \u00a0\u00a0 - same type name: <code>GitVersionInformation</code> \u00a0\u00a0 - same namespace behavior as GitVersion default (global namespace) \u00a0\u00a0 - fields as compile-time constants as GitVersion commonly emits (<code>public const string ...</code>). Example structure appears in GitVersion issue discussions around generated <code>GitVersionInformation.g.cs</code>. 56</li> <li>Provide default GitFlow branch semantics: \u00a0\u00a0 - main/master: stable release versions (no prerelease label) \u00a0\u00a0 - release/ and hotfix/ branches: <code>-beta</code> prereleases \u00a0\u00a0 - all other branches (including develop, feature/, bugfix/, etc.): <code>-alpha</code> prereleases</li> <li>Produce a unique version string for every commit.</li> </ol>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#22-non-goals-explicitly-out-of-scope","title":"2.2 Non-Goals (Explicitly Out of Scope)","text":"<ul> <li>Supporting multiple branching strategies beyond GitFlow (e.g., GitHubFlow, trunk-based) out of the box.</li> <li>Advanced GitVersion configuration options, plugins, or \u201cmodes\u201d beyond what\u2019s needed here.</li> <li>Rewriting/patching <code>AssemblyInfo.cs</code> files on disk; versions should be injected at build time.</li> <li>Full parity with GitVersion\u2019s entire variable set beyond what is required for compatibility (but we will match the commonly-used variables list).</li> </ul>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#3-high-level-approach","title":"3. High-Level Approach","text":"<p>Deliver a NuGet package that:</p> <ul> <li>imports MSBuild targets automatically (including <code>buildTransitive</code> so repo-level install applies to all projects),</li> <li>runs a version computation step early in the build,</li> <li>sets MSBuild properties used for: \u00a0 - assembly stamping (version attributes) \u00a0 - packaging (<code>PackageVersion</code>)</li> <li>emits: \u00a0 - a generated <code>GitVersionInformation.g.cs</code> into <code>IntermediateOutputPath</code> \u00a0 - build log output summarizing computed versions</li> <li>optionally emits CI-friendly service messages (TeamCity, GitHub Actions) as plain log lines.</li> </ul> <p>The GitVersion MSBuild approach injects version metadata into assemblies by generating a temporary assembly info file at build time. 1 </p> <p>It may be worth exploring alternative approaches, as GitVersion's approach is occasionally problematic. We can ignore legacy .NET Framework builds and focus on building SDK-style projects targetting .NETStandard2.0 and .Net 8.0 and up. This may release us from backward-compatibility constraints imposed by .NET Framework. However, if no clearly superior approach can be found, we must adopt the same pattern as GitVersion but keep configuration and behaviour minimal.</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#4-packaging-distribution","title":"4. Packaging &amp; Distribution","text":""},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#41-nuget-packages","title":"4.1 NuGet Packages","text":"<p>Primary package: <code>Timtek.GitFlowVersioning</code></p> <p>Contents: - <code>build/Timtek.GitFlowVersioning.props</code> - <code>build/Timtek.GitFlowVersioning.targets</code> - <code>buildTransitive/Timtek.GitFlowVersioning.props</code> - <code>buildTransitive/Timtek.GitFlowVersioning.targets</code> - MSBuild task assemblies (multi-targeted) - any helper executables if needed (optional; see below)</p> <p>Optional secondary package: <code>Timtek.GitFlowVersioning.Tool</code> (dotnet tool)\u00a0</p> <p>Not required for MSBuild integration, but can: - print variables as JSON (GitVersion-like) - validate repo state - aid debugging in CI</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#42-installation-no-csproj-edits-preferred","title":"4.2 Installation (No <code>.csproj</code> edits preferred)","text":"<p>Supported \u201cminimal-touch\u201d installs:</p> <ol> <li>Central Package Management: add package version to <code>Directory.Packages.props</code>, and reference once in <code>Directory.Build.props</code> (repo-level).</li> <li>Directory.Build.props: add a single <code>PackageReference</code> (or <code>PackageVersion</code>) that flows to all SDK-style projects via <code>buildTransitive</code>.</li> </ol> <p>Note: A completely zero-touch install (no repo file change at all) is not feasible with NuGet/MSBuild alone; MSBuild must import the targets somehow. The design goal is \u201cno per-project <code>.csproj</code> edits,\u201d not \u201cno repo edits.\u201d</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#5-compatibility-targets","title":"5. Compatibility Targets","text":""},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#51-msbuild-task-target-frameworks","title":"5.1 MSBuild Task Target Frameworks","text":"<p>To support Visual Studio desktop MSBuild and dotnet msbuild, the MSBuild task assemblies should multi-target:</p> <ul> <li><code>net472</code> or <code>net48</code> (Visual Studio MSBuild typically runs on .NET Framework)</li> <li><code>netstandard2.0</code> (consumable by dotnet MSBuild task host)</li> <li>optionally <code>net8.0</code> for modern tool host performance (not required)</li> </ul> <p>This avoids the incompatibility noted in GitVersion\u2019s newer MSBuild task docs regarding Visual Studio\u2019s MSBuild runtime. 1</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#52-git-interaction-strategy-cross-platform","title":"5.2 Git Interaction Strategy (Cross-Platform)","text":"<p>To work reliably on Windows/Linux and avoid native library deployment issues, the default Git interaction SHOULD use the <code>git</code> CLI (invoked as a process), not libgit2 bindings.</p> <p>Assumption: build agents have <code>git</code> installed (standard in TeamCity and GitHub-hosted runners).\u00a0</p> <p>If computing a version string is not possible for any reason, then an explanatory note shall be emitted into the build log, and the version string shall be \"0.0.0-unversioned\".</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#6-inputs-configuration","title":"6. Inputs &amp; Configuration","text":""},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#61-configuration-file","title":"6.1 Configuration File","text":"<p>There shall be no configuration file. This utility is opinionated, zero-configuration, and shall adopt Git Flow default semantics.</p> <ul> <li><code>mainBranchNames</code>: [<code>main</code>, <code>master</code>]</li> <li><code>developBranchName</code>: <code>develop</code></li> <li><code>tagPrefixRegex</code>: no prefix</li> <li><code>versionTagRegex</code>: <code>^(?&lt;prefix&gt;[vV]?)(?&lt;version&gt;\\d+\\.\\d+\\.\\d+)$</code></li> <li><code>fallbackBaseVersion</code>: <code>0.1.0</code></li> </ul>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#62-msbuild-properties-overrides","title":"6.2 MSBuild Properties (Overrides)","text":"<p>Allow overriding via MSBuild properties:</p> <ul> <li><code>GitFlowVersioningEnabled</code> (default <code>true</code>)</li> </ul>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#7-outputs-version-variables","title":"7. Outputs (Version Variables)","text":"<p>The tool MUST compute and expose a set of variables aligned with GitVersion\u2019s \u201cVersion Variables\u201d reference. 2</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#71-canonical-variables-minimum-set","title":"7.1 Canonical Variables (Minimum Set)","text":"<p>At minimum, compute:</p> <ul> <li><code>Major</code>, <code>Minor</code>, <code>Patch</code> 2 </li> <li><code>BranchName</code>, <code>EscapedBranchName</code> 2 </li> <li><code>Sha</code>, <code>ShortSha</code> 2 </li> <li><code>MajorMinorPatch</code> 2 </li> <li><code>SemVer</code>, <code>FullSemVer</code> 2 </li> <li><code>PreReleaseLabel</code>, <code>PreReleaseLabelWithDash</code> 2 </li> <li><code>PreReleaseNumber</code>, <code>PreReleaseTag</code>, <code>PreReleaseTagWithDash</code> 2 </li> <li><code>BuildMetaData</code>, <code>FullBuildMetaData</code> 2 </li> <li><code>InformationalVersion</code> (used for <code>AssemblyInformationalVersion</code>) 2 </li> <li><code>AssemblySemVer</code> and <code>AssemblySemFileVer</code> with GitVersion-like defaults:\u00a0 \u00a0 - <code>AssemblySemVer</code> defaults to <code>Major.Minor.0.0</code> 2\u00a0 \u00a0 - <code>AssemblySemFileVer</code> defaults to <code>Major.Minor.Patch.0</code> 2 </li> </ul> <p>Note: GitVersion\u2019s variable documentation provides these defaults and intended usage. 2</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#72-uniqueness-rule","title":"7.2 Uniqueness Rule","text":"<p>Commits on master/main branches are assumed to be deployable releases, therefore should not have any prerelease tag. Every commit shall increment the Patch version unless overriden by a tag.</p> <p>For other branches, the tool MUST produce a unique version for every commit. This is achieved by:</p> <ul> <li> <p>using a commit-distance based numeric component (e.g., <code>PreReleaseNumber</code> or <code>BuildMetaData</code>), and</p> </li> <li> <p>always including a SHA-bearing <code>FullBuildMetaData</code> component. GitVersion\u2019s <code>FullBuildMetaData</code> concept explicitly combines build metadata with branch and SHA. 2</p> </li> </ul>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#8-gitflow-versioning-semantics-default-behavior","title":"8. GitFlow Versioning Semantics (Default Behavior)","text":"<p>This section defines the default behavior required by the user and consistent with GitFlow concepts described by GitVersion. 34</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#81-branch-classification","title":"8.1 Branch Classification","text":"<p>Determine <code>BranchType</code> by case-insensitive <code>BranchName</code>:</p> <ul> <li>Main branch: <code>main</code> or <code>master</code></li> <li>Develop branch: <code>develop</code></li> <li>Release branches: <code>release/*</code></li> <li>Hotfix branches: <code>hotfix/*</code></li> <li>Other branches: everything else (feature/, bugfix/, etc.)</li> </ul> <p>GitVersion\u2019s GitFlow docs describe release and hotfix branch prefixes and that tags on main reflect stable releases. 3</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#82-version-source","title":"8.2 Version Source","text":"<p>Compute a \u201cversion source\u201d commit and base version:</p> <ol> <li>Find the latest SemVer tag reachable from the current commit (preferably from main\u2019s first-parent history). GitVersion\u2019s GitFlow docs treat tags on main as stable releases. 3 </li> <li>Parse <code>Major.Minor.Patch</code> from the tag.</li> <li>If no tag exists, use <code>fallbackBaseVersion</code> (default <code>0.1.0</code>), aligning with GitVersion\u2019s GitFlow doc fallback behavior description for main when no merge/tag is found. 3 </li> </ol> <p>Compute:</p> <ul> <li><code>VersionSourceSha</code></li> <li><code>CommitsSinceVersionSource</code> (a.k.a. distance / height)</li> </ul>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#83-mainmaster-behavior-stable","title":"8.3 Main/Master Behavior (Stable)","text":"<p>On main/master:</p> <ul> <li><code>PreReleaseLabel</code> MUST be empty.</li> <li><code>FullSemVer</code> SHOULD be <code>Major.Minor.Patch</code>.</li> <li>For non-tagged commits on main/master, the tool MUST still be unique per commit by incrementing the Patch version (note: build metadata is not part of the semantic version and cannot therefore produce uniqueness)</li> <li><code>InformationalVersion</code> should follow the GitVersion definition: defaults to <code>FullSemVer</code> suffixed by <code>FullBuildMetaData</code>. 2 </li> </ul>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#84-release-and-hotfix-behavior-beta","title":"8.4 Release/ and Hotfix/ Behavior (Beta)","text":"<p>On release/ and hotfix/**:</p> <ul> <li> <p><code>PreReleaseLabel</code> MUST be <code>beta</code></p> </li> <li> <p><code>PreReleaseNumber</code> MUST be commit distance from <code>VersionSourceSha</code> (or branch-local height)</p> </li> <li> <p><code>SemVer</code> MUST include <code>-beta.{n}</code></p> </li> <li> <p><code>FullSemVer</code> MUST include the prerelease and may include build metadata</p> </li> <li> <p>This aligns with GitVersion\u2019s GitFlow doc which describes hotfix branches producing beta prereleases with an incrementing count. 3 </p> </li> </ul>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#85-all-other-branches-alpha","title":"8.5 All Other Branches (Alpha)","text":"<p>On develop and all other non-main branches:</p> <ul> <li> <p><code>PreReleaseLabel</code> MUST be <code>alpha</code></p> </li> <li> <p><code>PreReleaseNumber</code> MUST ensure uniqueness (commit-distance based)</p> </li> <li> <p>GitVersion\u2019s GitFlow doc describes develop as <code>alpha.{n}</code> and minor bump relative to main. 3\u00a0 \u00a0 - This tool MAY adopt the same \u201cdevelop is next minor\u201d rule, but since requirements only mandate alpha/beta labeling, the \u201cnext minor\u201d strategy is configurable.</p> </li> </ul> <p>- Default behavior SHOULD follow GitVersion\u2019s GitFlow approach:</p> <p>- Base version from main</p> <p>- develop uses <code>Minor = mainMinor + 1</code>, <code>Patch = 0</code>, prerelease <code>alpha.{n}</code> 3 </p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#9-build-integration-msbuild","title":"9. Build Integration (MSBuild)","text":""},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#91-targets-execution-points","title":"9.1 Targets &amp; Execution Points","text":"<p>The <code>.targets</code> file MUST:</p> <ol> <li>Run version calculation before compilation and packing:</li> </ol> <p>- Before <code>CoreCompile</code></p> <p>- Before <code>GenerateAssemblyInfo</code> (or override it)</p> <p>- Before <code>Pack</code></p> <ol> <li>Generate:</li> </ol> <p>- <code>GitVersionInformation.g.cs</code> in <code>$(IntermediateOutputPath)</code></p> <p>- <code>Timtek.GitFlowVersioning.AssemblyInfo.g.cs</code> (or set equivalent MSBuild properties)</p> <p>GitVersion\u2019s MSBuild task generates a temporary <code>AssemblyInfo.cs</code> at build time containing the appropriate version attributes. 1</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#92-assembly-stamping","title":"9.2 Assembly Stamping","text":"<p>The tool MUST stamp:</p> <ul> <li> <p><code>AssemblyVersion</code> \u2190 <code>AssemblySemVer</code> 12 </p> </li> <li> <p><code>AssemblyFileVersion</code> \u2190 <code>AssemblySemFileVer</code> 2 </p> </li> <li> <p><code>AssemblyInformationalVersion</code> \u2190 <code>InformationalVersion</code> 12 </p> </li> </ul> <p>Implementation options:</p> <ul> <li> <p>Generate an assembly info source file with <code>[assembly: ...]</code> attributes.</p> </li> <li> <p>Or set MSBuild properties (<code>AssemblyVersion</code>, <code>FileVersion</code>, <code>InformationalVersion</code>) and rely on SDK targets.</p> </li> </ul> <p>Because the requirement states \u201cideally no csproj modifications,\u201d the tool SHOULD prefer the property route when possible, and fall back to generated source when SDK behaviors differ.</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#93-nuget-package-versioning","title":"9.3 NuGet Package Versioning","text":"<p>For <code>dotnet pack</code>:</p> <ul> <li>Set <code>PackageVersion</code> to a SemVer2 string derived from:</li> </ul> <p>- stable main: <code>Major.Minor.Patch</code> for tagged commits; otherwise a unique semver2 string</p> <p>- beta/alpha: <code>Major.Minor.Patch-{label}.{n}</code></p> <ul> <li>Set <code>Version</code> (optional) consistently with <code>PackageVersion</code>.</li> </ul> <p>GitVersion\u2019s documentation emphasizes that version variables are meant to be used for versioning assemblies and packages. 72</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#94-build-log-output","title":"9.4 Build Log Output","text":"<p>On every build, log a concise summary:</p> <ul> <li> <p><code>FullSemVer</code></p> </li> <li> <p><code>SemVer</code></p> </li> <li> <p><code>AssemblySemVer</code></p> </li> <li> <p><code>AssemblySemFileVer</code></p> </li> <li> <p><code>InformationalVersion</code></p> </li> <li> <p><code>BranchName</code></p> </li> <li> <p><code>Sha</code></p> </li> </ul> <p>This must be visible in:</p> <ul> <li> <p>Visual Studio Output window</p> </li> <li> <p><code>dotnet build</code> console output</p> </li> <li> <p>TeamCity and GitHub Actions logs</p> </li> </ul>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#10-ci-behavior","title":"10. CI Behavior","text":""},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#101-teamcity","title":"10.1 TeamCity","text":"<p>At minimum:</p> <ul> <li>log version summary lines (TeamCity will capture them).</li> <li>emit TeamCity service messages to set build number.</li> </ul> <p>GitVersion is commonly used to expose version variables to build servers like TeamCity. 78</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#102-github-actions","title":"10.2 GitHub Actions","text":"<p>At minimum:</p> <ul> <li> <p>log version summary lines.</p> </li> <li> <p>emit workflow commands in logs (design choice):</p> </li> </ul> <p>- <code>::notice title=Version::FullSemVer=...</code></p> <p>- <code>echo \"version=...\" &gt;&gt; $GITHUB_OUTPUT</code> (only if running in a step that expects it)</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#11-generated-gitversioninformation-static-class","title":"11. Generated <code>GitVersionInformation</code> Static Class","text":""},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#111-file-name-location","title":"11.1 File Name &amp; Location","text":"<p>Generate <code>GitVersionInformation.g.cs</code> under <code>$(IntermediateOutputPath)</code> and include it in compilation as <code>Compile</code> item.</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#112-namespace-rules-compatibility","title":"11.2 Namespace Rules (Compatibility)","text":"<ul> <li>Generate no namespace (global namespace), so consuming code can reference:</li> </ul> <p>- <code>GitVersionInformation.FullSemVer</code></p> <p>GitVersion issue reports show a generated file pattern with <code>static class GitVersionInformation</code> and const string fields under a chosen namespace, including an option named similarly to <code>UseProjectNamespaceForGitVersionInformation</code>. 5 We will always use the root namespace.</p>"},{"location":"Timtek.GitFlowVersion/GitFlowVersion%20Design%20Specification/#113-field-set","title":"11.3 Field Set","text":"<p>The class MUST contain constant fields matching the chosen variable set (\u00a77.1), using names identical to GitVersion variable names.</p> <p>GitVersion\u2019s \u201cVersion Variables\u201d page lists the canonical variable names and meanings (e.g., <code>FullSemVer</code>, <code>InformationalVersion</code>, <code>AssemblySemVer</code>). 2</p> <p>Example shape (illustrative only; values are placeholders):</p> <p>```csharp</p> <p>// </p> <p>using System.Runtime.CompilerServices;</p> <p>[CompilerGenerated]</p> <p>internal static class GitVersionInformation</p> <p>{</p> <p>public const string Major = \"1\";</p> <p>public const string Minor = \"2\";</p> <p>public const string Patch = \"3\";</p> <p>public const string PreReleaseLabel = \"alpha\";</p> <p>public const string PreReleaseLabelWithDash = \"-alpha\";</p> <p>public const string PreReleaseNumber = \"42\";</p> <p>public const string PreReleaseTag = \"alpha.42\";</p> <p>public const string PreReleaseTagWithDash = \"-alpha.42\";</p> <p>public const string BranchName = \"feature/foo\";</p> <p>public const string EscapedBranchName = \"feature-foo\";</p> <p>public const string Sha = \"0123456789abcdef...\";</p> <p>public const string ShortSha = \"0123456\";</p> <p>public const string MajorMinorPatch = \"1.2.3\";</p> <p>public const string SemVer = \"1.2.3-alpha.42\";</p> <p>public const string FullSemVer = \"1.2.3-alpha.42+99\";</p> <p>public const string BuildMetaData = \"99\";</p> <p>public const string FullBuildMetaData = \"99.Branch.feature/foo.Sha.0123456789abcdef...\";</p> <p>public const string InformationalVersion = \"1.2.3-alpha.42+99.Branch.feature/foo.Sha.012345...\";</p> <p>public const string AssemblySemVer = \"1.2.0.0\";</p> <p>public const string AssemblySemFileVer = \"1.2.3.0\";</p> <p>}</p>"},{"location":"Timtek.GitFlowVersion/Welcome/","title":"Welcome","text":"<p>This is your new vault.</p> <p>Make a note of something, [[create a link]], or try the Importer!</p> <p>When you are ready, delete this note and make the vault your own.</p>"}]}